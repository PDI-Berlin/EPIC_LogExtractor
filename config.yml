# ── EPIC Log Extractor Configuration ────────────────────────────────────────

# ── Log folder ──────────────────────────────────────────────────────────────
# Default log folder to process when no path is given on the command line.
# If empty or missing, the script will ask for a path interactively.
log_path: 'C:\Users\sina\Nextcloud\_share_Ebna\MBE_PC\Log extractor\2026_06_04'

# ── Output ──────────────────────────────────────────────────────────────────
# Where to write growth-run folders.
# Leave empty to use the default (log folder's parent).
output:
  base_path: 'C:\Users\sina\Nextcloud\Nomad\MBE_PC\LogFileReProcess\data'

# ── Auxiliary files ─────────────────────────────────────────────────────────
# Copied into each growth-run folder alongside EPIC_logs/.
# Paths are relative to this config file.
auxiliary_files:
  - 'C:\Users\sina\Nextcloud\Nomad\MBE_PC\LogFileReProcess\data\MBE8_config.xlsx'
  - 'C:\Users\sina\Nextcloud\Nomad\MBE_PC\LogFileReProcess\data\xrd-metafile.txt'

# ── NOMAD Upload ────────────────────────────────────────────────────────────
nomad:
  servers:
    sina:
      base_url: http://intra8-02.pdi-berlin.de/nomad-oasis/api/v1
      upload_id: gq9KCxllSQ6jgAgcDRf-tQ
